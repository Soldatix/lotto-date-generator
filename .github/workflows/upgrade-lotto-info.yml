name: Upgrade Date Lotto Info

on:
  push:
    paths:
      - '.github/workflows/upgrade-lotto-info.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upgrade Info / Donations
        run: python .github/scripts/upgrade-lotto-info.py
      - name: Verify
        run: |
          grep -q 'payment-availability' index.html
          grep -q 'Donate with PayPal' index.html
          grep -q 'Donate with Stripe' index.html
          grep -q 'digitalWallets' index.html
          ! grep -q 'apps-games-info-standard.js' index.html
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Upgrade Date Lotto Info design"
          git push
